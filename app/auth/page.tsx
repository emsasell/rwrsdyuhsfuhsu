'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {supabase} from '@/lib/supabase';
export default function Auth(){const [mode,setMode]=useState<'login'|'register'>('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[name,setName]=useState(''),[loading,setLoading]=useState(false),[err,setErr]=useState('');const r=useRouter();
async function go(e:React.FormEvent){e.preventDefault();setLoading(true);setErr(''); try{if(mode==='register'){const {data,error}=await supabase.auth.signUp({email,password,options:{data:{display_name:name||email.split('@')[0]}}});if(error)throw error;if(data.user) await supabase.from('profiles').upsert({id:data.user.id,display_name:name||email.split('@')[0],username:crypto.randomUUID().slice(0,8)}); if(!data.session)setErr('Проверь почту и подтверди аккаунт.');else r.replace('/chat')}else{const {error}=await supabase.auth.signInWithPassword({email,password});if(error)throw error;r.replace('/chat')}}catch(x:any){setErr(x.message||'Ошибка')}finally{setLoading(false)}}
return <main className="authPage"><div className="authGlow"/><form onSubmit={go} className="authCard"><div className="brandMark">E</div><h1>EMCHAT</h1><p>Быстрый, плавный и современный мессенджер</p>{mode==='register'&&<input placeholder="Ваше имя" value={name} onChange={e=>setName(e.target.value)}/>}<input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" minLength={6} placeholder="Пароль" value={password} onChange={e=>setPassword(e.target.value)} required/>{err&&<div className="error">{err}</div>}<button disabled={loading}>{loading?'Подождите...':mode==='login'?'Войти':'Создать аккаунт'}</button><button type="button" className="linkBtn" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Нет аккаунта? Регистрация':'Уже есть аккаунт? Войти'}</button></form></main>}
