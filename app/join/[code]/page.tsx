'use client'; import {useParams} from 'next/navigation'; import {useEffect,useState} from 'react'; import {supabase} from '@/lib/supabase';
export default function JoinPage(){const params=useParams<{code:string}>();const [status,setStatus]=useState('Подключаемся…');useEffect(()=>{(async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user){location.href='/auth';return}const {data,error}=await supabase.rpc('join_chat_by_invite',{p_code:params.code});if(error){setStatus(error.message);return}location.href='/chat?chat='+String(data)})()},[params.code]);return <main className="joinPage"><div className="joinCard"><h1>EMCHAT</h1><p>{status}</p></div></main>}
